# hw-demo Tasks Done — 2026-03-22

_Items moved here when fully implemented and verified._

## All 4 tasks completed — commit `1938e4f`

1. **⚙ Session Settings panel** — AI Persona collapsed into collapsible panel (default closed). Green dot indicator when prompt set. "⬇ Fetch API Data" button when `contextApiUrl` is configured for the session.

2. **Magic link** — "🔗 Link" button in workspace navbar, copies `?hub=<sessionId>&lock=true`. `lock=true` hides back-to-dashboard button and prevents session switching (`hubLocked` state in page.jsx).

3. **Platform pop-out** — Iframe expanded to 1100px. "Pop out ⤢" button creates `position:fixed` full-screen overlay. CSS `.ws-platform-popped` override removes `max-height: 260px` constraint on `wkp-platform-frame`. "Close ✕" dismisses.

4. **Therme tiles fix** — `researchFixed` override in `demo_sessions.json` + server patch: Revenue EUR 48.5M, Employees 580, Market Cap Private.
